import { createContext } from "react";

/**
 * Contexto de autenticación para la aplicación
 * Este archivo sólo contiene la definición del contexto, sin el componente Provider
 */
export const AuthContext = createContext(null);
